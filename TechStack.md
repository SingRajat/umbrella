Frontend->streamlit
backend->fastapi
orchestration->langchain
vector database->chromadb
llm->grok api
database->postgres SQL(not now,we can move later)
->project is statless so no authentication,athorization,oAuth,JWT etc.
caching->inbuilt streamlit or redis?

IMP: Ask me if i have left something,dont fill the gaps blindly ,never make assumptions

Note:Create the UI basic for now,later i will give you DESIGN.md for better UI implementation.